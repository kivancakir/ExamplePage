export interface MenuTypes {
    id: number,
    name: string,
    icon: any
}

export interface ShiftTemplateTypes {
    id: number,
    timeInterval: string,
    job: string
}

export interface WeekTemplateTypes {
    id: number,
    time: string,
    byWhom: string
}